# TensileLite "New Build Algo" — Decomposition Recovery: Issue Plan

Recover the `nba-final` layered decomposition of `TensileCreateLibrary` (slim `Run.py` + `IO.py` / `Logic.py` / `Tuning.py`) onto the current develop 1138-LOC monolithic `Run.py`.

- **Package:** `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/`
- **Design template (read-only reference):** `~/projects/archive/hipblastlt-worktrees/nba-final/tensilelite/Tensile/TensileCreateLibrary/`
- **Generated by:** ultracode workflow (8 agents: 4 analysis, 3 design proposals, 1 synthesis).

## Strategy

Synthesis of proposals 1 and 3 (proposal 2 is a stub, discarded). Port nba-final's layered decomposition (IO.py / Logic.py / Tuning.py + slim Run.py) onto develop's 1138-LOC Run.py using strict extract-then-reexport: every extraction commit moves a cohesive symbol group to a new sibling module AND in the SAME commit adds `from .NewModule import <names>` back into Run.py, so `import_module('Tensile.TensileCreateLibrary.Run').<sym>`, the harness `from ...Run import <sym>` chokepoint, the __init__ facade re-exports, and the __main__.run attr all keep resolving at every atomic commit (RULES A/B/F by construction). VERIFIED load-bearing facts: __init__.py re-exports exactly 6 names from .Run; ClientWriter/BenchmarkProblems/GenerateSummations import those names from the PACKAGE ROOT (.TensileCreateLibrary), which is never touched; _codegen+LocalRead harnesses import generateKernelObjectsFromSolutions/generateKernelHelperObjects/processKernelSource via `from Tensile.TensileCreateLibrary.Run import` (positional chokepoint, RULE G); run() calls writeSolutionsAndKernelsTCL (line 1036) and passPostKernelInfoToLibrary (line 1065) — TCL is the LIVE emitter, not the non-TCL public one. Two contradictions between the source proposals are resolved in favor of never-break-the-build: (1) proposal 1 MISSED that passPostKernelInfoToLibrary is only ever patch.object'd no-op (zero behavioral assertion) — I adopt proposal 3's hard Stage-0 pin (NBA-0c) and make NBA-10 depend on it; (2) the RULE D stinky/LibraryIO monkeypatch landmine: proposal 3's NBA-6 wrongly claims tests stay green because Run keeps the import, but a moved function resolves free vars in ITS OWN module namespace, so monkeypatch.setattr(M, 'verify_stinky_paths'/'_stinky_out'/'isaToGfx') and setattr(M.LibraryIO,'write') would silently no-op once the function lives in IO/Logic. VERIFIED: orchestration test patches M.verify_stinky_paths, M._stinky_out, M.isaToGfx, M.globalParameters, M.LibraryIO.write. Resolution: extractions that move a function reading a monkeypatched global RETARGET those monkeypatches to the new module in the SAME commit (RULE C explicitly permits same-commit test edits for a move), and Run.py retains the top-level os/shutil/LibraryIO/globalParameters imports for any patch target still read by an in-Run function. The shared NamedTuple/worker contract (KernelCodeGenResult, KernelMinResult, processKernelSource, generateKernelHelperObjects) STAYS defined in Run.py with the back-import block at the BOTTOM (after the type defs) so IO/Tuning can `from .Run import` them acyclically. Parallelism: after the Stage-0 pins and a single module-scaffold commit, the leaf groups (path->IO, stinky->IO, mem->IO, kernel-objects->Logic, fallback-rename->Logic) extract concurrently (disjoint Run line ranges, disjoint test files); the two emitters and the post-kernel passes serialize on Tuning.py and the contract. Per-commit gate = import-smoke (python -c 'import Tensile.TensileCreateLibrary as m; m.run') run FIRST, then the named char suite; full `tox -e unit -- -m unit` once per stage against the CONFIRMED develop baseline = **4974 passed / 220 skipped / 0 failed** (NBA-0b, this worktree; supersedes the stale DECISIONS.md D15 figure 2466/201, which under-counted ~2x).

## Invariant: every commit stays green

From projects/hipblaslt/tensilelite: (1) export ROCM_PATH=${ROCM_PATH:-/opt/rocm}; export LD_LIBRARY_PATH=$ROCM_PATH/lib:$LD_LIBRARY_PATH (skip if /opt/rocm/lib is ldconfig-registered); (2) import smoke: pip install ./rocisa/ && PYTHONPATH=$PWD python -c \"import Tensile.TensileCreateLibrary as m; m.run\" ; (3) targeted char suite for the touched module: tox -e unit -- Tensile/Tests/unit/characterization/<AffectedModule>/ . If all three pass, the single issue did not break import resolution, the bin/cmake run() contract, or the affected behavior. Run the full no-regression gate (tox -e unit -- -m unit Tensile/Tests/unit) once per batch, not per commit (DECISIONS.md D1). Do not push; commit locally and prove green first (memory: no-push/local-proof-first).

**Test gate commands:**

```
(a) IMPORT SMOKE — rocisa is a HIP-linked nanobind ext, so its .so needs the ROCm runtime on the loader path or import aborts (the tox envs set this for you):  cd projects/hipblaslt/tensilelite && export ROCM_PATH=${ROCM_PATH:-/opt/rocm} && export LD_LIBRARY_PATH=$ROCM_PATH/lib:$LD_LIBRARY_PATH && pip install ./rocisa/ && PYTHONPATH=$PWD python -c \"import rocisa, Tensile, Tensile.TensileCreateLibrary; from Tensile.TensileCreateLibrary import run; print('import-smoke OK')\"   (prefer ldconfig-registered /opt/rocm/lib in the dev image so no LD_LIBRARY_PATH is needed — see memory rocm-image-ldconfig-requirement). (b) CHARACTERIZATION / UNIT SUITE — fast, no client build, the tox env already exports ROCM_PATH + LD_LIBRARY_PATH and pip-installs rocisa:  tox -e unit -- -m unit Tensile/Tests/unit   To scope to affected modules only, pass dirs:  tox -e unit -- Tensile/Tests/unit/characterization/<Module>/ {posargs}   For coverage of a single module use the dedicated env (NEVER point --cov at a bare module path): tox -e coverage-unit -- Tensile/Tests/unit/characterization/<Module>/ . COVERAGE GOTCHA: pytest-cov with a --cov arg that resolves to a file/non-dir triggers a rocisa nanobind double-import -> SIGABRT; always pass --cov=Tensile (a package/dir), which is what coverage-unit does. Full no-regression gate (run once per batch, not per commit): tox -e unit -- -m unit Tensile/Tests/unit  expecting the CONFIRMED green count = 4974 passed / 220 skipped / 0 failed (NBA-0b, this worktree). The stale DECISIONS.md D15 figure (2466/201) under-counted ~2x and must NOT be used as the gate.
```

## Import-stability rules (the backbone)

1. Run.py is a single module (Tensile/TensileCreateLibrary/Run.py), NOT a Run/ package. There is no Tensile.TensileCreateLibrary.Run.* submodule today; every symbol in runInternalImports is defined directly in this one file. A port that splits Run.py into a Run/ package or extracts modules MUST keep Tensile.TensileCreateLibrary.Run importable AND exposing all listed names at top level.
2. RULE A (backbone): every symbol imported via 'from Tensile.TensileCreateLibrary.Run import X' or accessed via import_module('Tensile.TensileCreateLibrary.Run').X MUST remain importable from .Run at EVERY commit. To move a symbol to a new module, re-import it back into the Run namespace in the SAME commit (from .NewModule import X inside Run.py) OR update the consumer in the same commit. Never land a commit where the symbol left Run but a consumer still imports it from Run.
3. RULE B (package-API surface): __init__.py re-exports exactly {copyStaticFiles, libraryDir, libraryRoot, run, tensileLibraryFile, writeSolutionsAndKernels} from .Run. If Run.py stops defining any of these, __init__.py must be updated in the same commit so 'from .TensileCreateLibrary import ...' keeps working for ClientWriter.py, BenchmarkProblems.py, GenerateSummations.py. Removing/renaming an __init__ export breaks 3 source consumers.
4. RULE C (test-bound privates are load-bearing): tests bind private names (_checkInvalidSolutions, _checkInvalidSolutionsAndKernels, _renameFallbackPlaceholders, _stinky_out, _stinky_asm_verify_wanted, _verify_stinky_asm_comment_vs_elf_text) and internal helpers (KernelMinResult, passPostKernelInfoToSolution, removeInvalidSolutionsAndKernels, memCompress/memDecompress, writeAssembly/writeHelpers/writeSolutionsAndKernelsTCL, generateLogicDataAndSolutions) via M=import_module('...Run'). The underscore prefix does NOT make them safe to rename; renaming requires editing the test in the same commit.
5. RULE D (module aliases stay on the Run object): tests monkeypatch M.os, M.shutil, M.LibraryIO and read M.globalParameters. These are module-level imports inside Run.py. If a refactor moves those call sites to a submodule, M.os/M.shutil/M.LibraryIO/M.globalParameters must still resolve as attributes of the Run module or test_run_orchestration_char.py and test_r7_createlib_deep_char.py break.
6. RULE E (sibling-module decoupling): Run.py CONSUMES ParallelMap2 (Tensile.Common.Parallel) and makeSourceToolchain/buildSourceCodeObjectFiles/makeAssemblyToolchain/buildAssemblyCodeObjectFiles (Tensile.Toolchain.Source/Assembly). The Parallel/, ToolchainSource/, ToolchainAssembly/ char suites target those source modules DIRECTLY, not Run.py, so they will stay green even if Run.py's import wiring to them breaks. Do not rely on them to cover the new build algorithm's parallel/codegen call sites; cover those via the TensileCreateLibraryRun/ suite plus a runtime import of Run.
7. RULE F (__main__ + bin entry): __main__.py does 'from Tensile.TensileCreateLibrary import run' and TensileLogicRun/test_tensile_logic_char.py asserts __main__ has attr 'run'. The package must keep a top-level callable 'run'. The bin/TensileCreateLibrary entry (referenced by ClientWriter.py:283, GenerateSummations.py:54, Tensile/cmake/TensileConfig.cmake, cmake/tensilelite_auto_build.cmake) drives this path; keep the console-script/module entry intact.
8. RULE G (codegen harness is a shared chokepoint): _codegen/codegen_harness.py and _codegen/config_harness.py import generateKernelObjectsFromSolutions, generateKernelHelperObjects, processKernelSource and are imported transitively by the _codegen/test_r* and LocalRead/test_r3 suites. Breaking any of these 3 functions' signatures cascades across the whole _codegen + LocalRead family. processKernelSource's signature (kernelWriterAssembly, data, outOptions, splitGSU, kernel, compress=False) -> KernelCodeGenResult is depended on positionally.
9. RULE H (per-commit verification): the green-build invariant is that 'import Tensile.TensileCreateLibrary; import Tensile.TensileCreateLibrary.Run; import Tensile.TensileCreateLibrary.__main__' succeeds AND pytest collects without ImportError for test_library_paths.py, test_perArchFallbackRename.py, characterization/TensileCreateLibraryRun, characterization/ParseArguments, characterization/_codegen, and characterization/LocalRead. Run this after each commit of the port, not only at the end.
10. RULE I (back-import placement — ONE precise mechanical rule, supersedes every looser phrasing like "near the bottom of the import block" or "after the KernelMinResult def"): ALL sibling-module back-imports live in a SINGLE contiguous block at the END of Run.py, AFTER the `def run():` body (i.e. at EOF). Rationale: every back-imported name is only ever CALLED at runtime (inside a function body), never referenced at module-load time, so importing them last is safe; and by EOF all four shared-contract names (KernelCodeGenResult @132, KernelMinResult @144, processKernelSource @216, generateKernelHelperObjects @699) are already bound, so when the EOF block runs `from .IO/.Tuning import ...` — which transitively triggers IO/Tuning's `from .Run import KernelMinResult/KernelCodeGenResult/processKernelSource` — those resolve against a fully-defined (partially-initialized only w.r.t. the back-imports themselves) Run module. The cycle is broken by this ordering. Each issue APPENDS its `from .NewModule import <names>` line(s) to this one EOF block, one logical import per issue (one name per line to keep concurrent group-D appends conflict-free). import-smoke (`python -c 'import Tensile.TensileCreateLibrary.Run'`) is the FIRST gate after any commit that touches this block.

## Build hooks that must keep resolving

- cmake/tensilelite_auto_build.cmake VALID_BINS list: must keep entries 'TensileCreateLibrary','Tensile','TensileBenchmarkCluster','TensileGenerateSummations','TensileLogic','TensileRetuneLibrary','TensileUpdateLibrary','TensileMergeLibrary' — set_tensile_bin() FATAL_ERRORs on any name not in this list
- cmake/tensilelite_auto_build.cmake: TENSILE_BIN_ROOT = ${CMAKE_CURRENT_SOURCE_DIR}/Tensile/bin must point at the dir containing the launcher scripts; RunPythonScript custom_target invokes ${TENSILE_BIN_ROOT}/${TENSILE_BIN} and add_dependencies(RunPythonScript rocisa) — the 'rocisa' target must exist
- Tensile/bin/TensileCreateLibrary (executable launcher): does `from Tensile import TensileCreateLibrary` then calls TensileCreateLibrary.run() — both the import path and run() symbol must resolve; this is the script CMake actually execs
- Tensile/cmake/TensileConfig.cmake TensileCreateLibraryFiles(): builds CommandLine = ... ${VIRTUALENV_BIN_DIR}/${VIRTUALENV_PYTHON_EXENAME} ${Script} ... where Script defaults to ${Tensile_ROOT}/bin/TensileCreateLibrary (or ${Tensile_TENSILE_ROOT}/bin/TensileCreateLibrary). That bin/TensileCreateLibrary path plus PYTHONPATH=${PROJECT_BINARY_DIR}/lib must keep resolving; the trailing 'HIP' positional and the --architecture/--jobs/--code-object-version/etc. flag names are the CLI contract TensileCreateLibrary.run() must continue to accept
- Tensile/TensileCreateLibrary/__init__.py: `from .Run import ... run ...` — the public symbol `run` (and copyStaticFiles, libraryDir, libraryRoot, tensileLibraryFile, writeSolutionsAndKernels re-exported here) must stay exported from the package; pyproject [project.scripts] entry 'TensileCreateLibrary = "Tensile.TensileCreateLibrary:run"' and Tensile/TensileCreateLibrary/__main__.py (`from Tensile.TensileCreateLibrary import run`) both bind to it
- pyproject.toml [tool.setuptools.package-data] Tensile=["cmake/*"] and MANIFEST.in (recursive-include Tensile *.cmake; include Tensile/bin/Tensile Tensile/bin/TensileCreateLibrary): TensileConfig.cmake + the two launchers must remain packaged for installed-tree consumers (rocBLAS/hipBLASLt find_package(Tensile))
- tox.ini/coverage envs and pytest.ini drive the suite via `invoke build-client`/`invoke get-gpu-arch` and --prebuilt-client=build_tmp/tensilelite/client/tensilelite-client — the invoke task names and that client output path are build hooks the test gate depends on

## Parallelism map

- Group A (Stage 0, NBA-0/0b/0c/0d): ADR home + baseline + the two safety-net pins. NBA-0b depends on NBA-0 (same file). NBA-0c (passPostKernelInfoToLibrary pin) and NBA-0d (import-surface pin) are independent test-file additions, mutually concurrent. NBA-0c is on the critical path: it gates NBA-10.
- Group B (Stage 0, NBA-0e): TCL return-shape pin + __init__ normalization; depends on NBA-0 for the ADR; independent of group A's test pins (different test file edits, though NBA-0e and NBA-0c both touch test files in the same dir — land in quick succession to avoid a merge in those files).
- Group C (Stage 1, NBA-1): single serialization point — the only commit that introduces the three empty modules. Everything in Stage 2+ depends on it; cannot parallelize internally.
- Group D (Stage 2, NBA-2/3/4/5/6/7/8): the maximum-parallelism set. NBA-2 (path->IO), NBA-4 (mem->IO), NBA-5 (validity->Tuning), NBA-6 (fallback->Logic), NBA-8 (kernel-objects->Logic) are fully independent (disjoint Run line ranges, different new-file regions, different test files). NBA-3 (stinky->IO) depends on NBA-2 only to order IO.py edits and carries a same-commit orchestration-test monkeypatch retarget. NBA-7 (write-primitives->IO) depends on NBA-4 (needs IO-resident memDecompress) + the KernelMinResult define-before-backimport ordering. CAVEAT: NBA-2/3/4/7 all edit IO.py and all append to Run.py's import block — runnable by different agents but require trivial append-only import-block merges; truly conflict-free only if each appends its back-import on its own line. Land them in quick succession rather than literally simultaneously.
- Group E (Stage 3, NBA-9/10/11): NBA-9 (copyStaticFiles->IO, depends NBA-2) is parallel to the Logic/Tuning work. NBA-10 (passPostKernelInfoToLibrary->Tuning, depends NBA-0c pin + NBA-5 ordering) and NBA-11 (generateLogicDataAndSolutions->Logic, depends NBA-6 + same-commit LibraryIO retarget) target different modules so are mutually parallel.
- Group F (Stage 4, NBA-12/13): the two emitters ->Tuning. NBA-12 (live TCL, depends NBA-2/3/5/7/0e — all sibling helpers must already be in IO/Tuning). NBA-13 (public non-TCL) serializes after NBA-12 (shared Tuning.py + Run import-block + same cycle technique). High-contention; one commit at a time.
- Group G (Stage 5, NBA-14/15): NBA-14 (doc-only placement decision) is independent; NBA-15 (slim + full gate) is the join point depending on all extractions.
- Group H (Stage 5, NBA-16): optional consumer-import cleanup; default-skippable because the Run shims work indefinitely.

## Global risks

- RULE D monkeypatch breakage is the dominant correctness risk and the point the source proposals contradicted each other on. VERIFIED: test_run_orchestration_char.py patches M.verify_stinky_paths, M._stinky_out, M.isaToGfx, M.globalParameters, and M.LibraryIO.write. A function resolves its free-variable lookups in ITS OWN module's __globals__, so once _verify_stinky_asm_comment_vs_elf_text / _stinky_asm_verify_wanted move to IO (NBA-3) and generateLogicDataAndSolutions moves to Logic (NBA-11), patching the Run module silently no-ops — the test would pass against UNPATCHED behavior or fail. Proposal 3's claim that 'Run keeps the import so M.x still works' is wrong for this reason. Resolution adopted: the moving commit RETARGETS those monkeypatches to the new module in the SAME commit (RULE C permits same-commit test edits for a move). Audit every moved function for reads of verify_stinky_paths/_stinky_out/isaToGfx/globalParameters/LibraryIO/os/shutil before extracting.
- Un-pinned passPostKernelInfoToLibrary (proposal 1 missed this entirely). VERIFIED it is only ever patch.object(M,...)'d to a no-op in test_r7 (8 sites) with zero behavioral assertion. Moving it without a real pin would be an untested relocation of develop-only logic. Mitigation: NBA-0c pins it first and is a hard dependency of NBA-10.
- CIRCULAR IMPORT. KernelCodeGenResult, KernelMinResult, processKernelSource, and generateKernelHelperObjects MUST stay defined in Run.py and the `from .IO/.Logic/.Tuning import` back-import block MUST sit at the BOTTOM of Run.py (after those defs), because IO (writeAssembly) and Tuning (both emitters) do `from .Run import KernelMinResult/processKernelSource/KernelCodeGenResult`. The cycle is broken only by partial-module-initialization ordering. Mitigation: run import-smoke (python -c 'import Tensile.TensileCreateLibrary.Run') as the FIRST gate of NBA-7/12/13 before any pytest.
- rocisa nanobind import abort: import-smoke can SIGABRT if ROCM_PATH/LD_LIBRARY_PATH are unset or if --cov is pointed at a non-dir (double-import). Always use tox envs (loader path set); never pass --cov=<file>; use tox -e coverage-unit (--cov=Tensile, a package dir) for coverage. A green pytest run is only meaningful if import-smoke passed first.
- LIVE-PATH vs public-API divergence. VERIFIED run() calls writeSolutionsAndKernelsTCL (line 1036), NOT writeSolutionsAndKernels; the non-TCL emitter is the package re-export consumed by BenchmarkProblems.py from the root and is NOT in run()'s path. Both must move to Tuning; neither may be deleted. NBA-13 needs the BenchmarkProblems import smoke + the NBA-0d package-re-export pin because the run() suite will not catch a break in the non-TCL emitter.
- RULE G signature chokepoint. VERIFIED _codegen/codegen_harness.py + config_harness.py import generateKernelObjectsFromSolutions / generateKernelHelperObjects / processKernelSource via `from Tensile.TensileCreateLibrary.Run import` (positional). processKernelSource and generateKernelHelperObjects STAY in Run (NBA-14); generateKernelObjectsFromSolutions moves to Logic with a byte-identical signature + back-import (NBA-8). Any signature drift cascades across _codegen + LocalRead.
- _checkInvalidSolutions is dead but test-bound and NOT deletable per user CLAUDE.md (no deleting dead code outside the active change). NBA-5 extracts it verbatim and flags it in the ADR for the owner.
- Import-block merge contention: group-D issues (NBA-2/3/4/7) and the emitter stages all append back-import lines to the same region of Run.py. Concurrent edits produce git conflicts in Run.py's import block even though the logic is independent. Mitigation: one back-import per line, append-only; land group-D issues in quick succession rather than literally simultaneously — the file-level independence is in the NEW modules, not in Run.py.
- libraryDir colon-strip semantics are matched at runtime by tensile_host.cpp with no Python-test signal. NBA-2 must preserve the exact path logic; a 'cleanup' refactor would break the C++ runtime probe silently.
- Baseline drift: develop merges have historically mass-broken char tests. The baseline has now been RE-CONFIRMED on this worktree (NBA-0b, commit recorded in ADR-0001 History) = 4974 passed / 220 skipped / 0 failed; this is the immutable figure NBA-15 compares against. The DECISIONS.md D15 figure (2466/201) is stale (~2x under-count) and must not be used.

## ADR home

projects/hipblaslt/tensilelite/docs/decisions/ (a NEW peer to the existing Tensile/Tests/unit/characterization/DECISIONS.md). The characterization DECISIONS.md is suite-scoped and add-only; the parallel-build refactor touches the build/codegen surface that file explicitly puts OUT of scope (D0), so ADRs need their own home rather than appending unrelated entries to a test-coverage log. Use the build-adjacent docs tree so build reviewers find them next to cmake/, not buried under Tests/unit/characterization/.

**Filename pattern:** NNNN-kebab-title.md (zero-padded 4-digit monotonic id matching the DECISIONS.md 'Dn' numbering ethos, e.g. 0001-parallel-build-algorithm.md). One ADR file per atomic commit; an ADR UPDATE is an edit to the relevant existing file plus a dated bullet under its History section. This keeps 'every commit ships an ADR or ADR update' lightweight: a new decision = one new short file; a refinement = one appended bullet.

---

## STATUS (2026-06-26)

Stage 0 is **COMPLETE and committed** on branch `users/davidd-amd/tensilelite-new-build-algo`:
- NBA-0  (916a38fc0fb) — ADR home + ADR-0001. NOTE: the ADR file is named
  `0001-tensilecreatelibrary-decomposition.md` (not the `0001-parallel-build-decomposition-strategy.md`
  proposed below); all "ADR 0001-*" references in this doc mean that file.
- NBA-0b (1f6fc790cd2) — confirmed baseline **4974/220/0**.
- NBA-0c (4a6e4824f89) — passPostKernelInfoToLibrary behavioral pin.
- NBA-0d (a36b5ae842e) — executable import-surface contract.
- NBA-0e (02ef4a7645b) — TCL return-shape pin + __init__ normalization.
- ADRs 0002/0003/0004 (a65e0e04c89) — Stage-0 pin rationale.

**NEXT ACTION: NBA-1** (scaffold IO.py/Logic.py/Tuning.py), then Stage 2 extractions.
Stage-2+ each issue carries a "Direct-call monkeypatch audit" block (see RULE I and the
per-issue retarget lists) produced by the nba-monkeypatch-audit pass; re-verify Run.py line
numbers against the live file before each extraction (Run.py shrinks as work proceeds).

## Issues by stage

## Stage 0

Establish the ADR home + per-commit green recipe, and close the two real safety-net gaps BEFORE any code moves: (a) the only develop-only function with zero behavioral assertion (passPostKernelInfoToLibrary, currently only patch.object'd no-op), and (b) an executable import-surface contract that fails fast if any extraction drops a .Run symbol, a package re-export, or the __main__.run attr. Also pin the live TCL emitter's real return contract. No production logic changes; the only production edit is a behavior-preserving __init__ normalization.

### NBA-0 — Create docs/decisions ADR home and record the extract-then-reexport strategy (ADR-0001)

- **Size:** XS | **Parallel group:** A | **Depends on:** none

**Summary.** Create projects/hipblaslt/tensilelite/docs/decisions/ (verified absent) and land ADR-0001 documenting the shell-Run extract-then-reexport strategy, the re-import-back invariant (RULES A/B/F), the RULE D monkeypatch-retarget rule, and the per-commit green recipe. Pure docs.

**Files touched:**
- `projects/hipblaslt/tensilelite/docs/decisions/0001-tensilecreatelibrary-decomposition.md`

**Steps:**
1. Create directory projects/hipblaslt/tensilelite/docs/decisions/ (confirmed it does not exist).
2. Write 0001 with the mandated template: Status proposed; Decision = move cohesive groups to IO/Logic/Tuning and re-import-back into Run in the same commit; Why citing Run.py 1138 LOC + the verified import_module attribute-access + harness `from ...Run import` chokepoint; Alternatives rejected (big-bang move + consumer rewrite; converting Run.py to a package); the RULE D rule (a function that moves but reads a monkeypatched global requires same-commit retarget of that monkeypatch to the new module); the per-commit green recipe (import-smoke FIRST, then targeted char suite); empty History.
3. Record that the shared contract (KernelCodeGenResult/KernelMinResult/processKernelSource/generateKernelHelperObjects) stays in Run.py and the back-import block sits at the bottom for acyclicity.

**Build safety.** Docs-only commit; zero imports change, so import-smoke and the full suite are trivially unaffected.

**Test strategy.** Run import-smoke (python -c 'import Tensile.TensileCreateLibrary as m; m.run') to confirm the baseline is green; no char test is exercised by a docs file.

**ADR action.** new ADR 0001-tensilecreatelibrary-decomposition.md (DONE)

**Acceptance criteria:**
- [ ] docs/decisions/0001-*.md exists and follows the template
- [ ] ADR records the re-import-back invariant and the RULE D retarget rule
- [ ] import-smoke prints OK

### NBA-0b — Confirm and pin the full-suite baseline count (ADR-0001 History)

- **Size:** XS | **Parallel group:** A | **Depends on:** NBA-0

**Summary.** Re-run the full no-regression gate on untouched develop and append the confirmed passed/skipped counts to ADR-0001 History as the immutable baseline; do not trust DECISIONS.md D15 (2466/201) blindly given develop merges have historically mass-broken char tests.

**Files touched:**
- `projects/hipblaslt/tensilelite/docs/decisions/0001-tensilecreatelibrary-decomposition.md`

**Steps:**
1. From projects/hipblaslt/tensilelite, run tox -e unit -- -m unit Tensile/Tests/unit and capture passed/skipped totals.
2. Append a dated History bullet to ADR-0001 with the exact confirmed counts; note whether they match D15's 2466/201.

**Build safety.** Docs-only edit; no code path changes.

**Test strategy.** This issue IS the full-suite run: tox -e unit -- -m unit Tensile/Tests/unit. Its output is the artifact.

**ADR action.** update ADR 0001 (History: confirmed baseline counts)

**Acceptance criteria:**
- [ ] Full -m unit run completes with no ImportError
- [ ] Confirmed baseline counts appended to ADR-0001 History

### NBA-0c — Add characterization pin for passPostKernelInfoToLibrary (the only un-asserted develop-only function)

- **Size:** S | **Parallel group:** A | **Depends on:** none

**Summary.** VERIFIED: passPostKernelInfoToLibrary is only ever patch.object(M, ...)'d to a no-op in test_r7 (8 sites) and has NO assertion on real behavior. Pin it against the in-place implementation now so it can be moved to Tuning later without silent regression. This is the single safety-net gap proposal 1 missed.

**Files touched:**
- `projects/hipblaslt/tensilelite/Tensile/Tests/unit/characterization/TensileCreateLibraryRun/test_run_helpers_char.py`

**Steps:**
1. Read Run.py 308-373 to capture exact behavior: propagation of occupancy/pgr/mathclk plus the develop-only sizeMapping field set (PrefetchGlobalRead, NonTemporalA/B/D, adaptiveGemmNTAB, WaveSeparateGlobalReadA/B, UnrollLoopSwapGlobalReadOrder, DirectToVgprA/B, cuoccupancy, mathclk) into BOTH masterLibrary.solutions and lazyLibraries, keyed by getKernelFileBase, with the KeyError diagnostic branch.
2. In test_run_helpers_char.py, add tests binding M.passPostKernelInfoToLibrary (via the existing M=import_module('...Run') handle) and calling it directly on a minimal fabricated (results, kernels, masterLibraries) fixture mirroring the existing passPostKernelInfoToSolution fixtures.
3. Assert each propagated field lands in both masterLibrary.solutions and lazyLibraries; add one negative case exercising the KeyError diagnostic path.
4. Run the TCLR suite; confirm new tests pass against the unmoved implementation.

**Build safety.** Test-only addition against the existing in-place function; no production symbol moves, so all import rules and the bin/cmake run() contract are untouched.

**Test strategy.** New cases in Tensile/Tests/unit/characterization/TensileCreateLibraryRun/test_run_helpers_char.py binding M.passPostKernelInfoToLibrary (RULE C). Closes the only behavioral gap.

**ADR action.** new ADR 0002-pin-passpostkernelinfotolibrary.md (proposed title: 'Pin passPostKernelInfoToLibrary before extraction')

**Acceptance criteria:**
- [ ] M.passPostKernelInfoToLibrary is exercised by at least one assertion on real (unpatched) output
- [ ] Both masterLibrary.solutions and lazyLibraries write-back asserted
- [ ] KeyError diagnostic branch covered
- [ ] TCLR suite green

### NBA-0d — Add an executable package-API + .Run import-surface pin

- **Size:** XS | **Parallel group:** A | **Depends on:** none

**Summary.** Lock the 6 public package re-exports, the .Run backbone symbol set, and the __main__.run attr in one guard test so any extraction that drops a symbol fails immediately at the source, not at a downstream consumer.

**Files touched:**
- `projects/hipblaslt/tensilelite/Tensile/Tests/unit/characterization/TensileCreateLibraryRun/test_run_orchestration_char.py`

**Steps:**
1. Add a test importing from the package root: from Tensile.TensileCreateLibrary import copyStaticFiles, libraryDir, libraryRoot, run, tensileLibraryFile, writeSolutionsAndKernels and asserting each is callable (these are the VERIFIED 6 names ClientWriter/BenchmarkProblems/GenerateSummations consume).
2. Add a test that import_module('Tensile.TensileCreateLibrary.Run') exposes every symbol later extractions will move (path helpers, _stinky_*, memCompress/Decompress, the validity passes, passPostKernelInfoToLibrary, the two emitters, generateKernelObjectsFromSolutions, renameFallbacksPerArch, generateLogicDataAndSolutions) via getattr.
3. Add a test that import_module('Tensile.TensileCreateLibrary.__main__') has attr 'run' (RULE F).
4. Run the TCLR suite; confirm green on the current tree.

**Build safety.** Pure additive guard test reading attributes that already exist; encodes RULES A/B/F as executable assertions. Cannot break the build.

**Test strategy.** New assertions in test_run_orchestration_char.py; complements test_library_paths.py and ParseArguments char tests.

**ADR action.** new ADR 0003-lock-import-surface.md (proposed title: 'Executable import-surface contract for the port')

**Acceptance criteria:**
- [ ] A failing extraction that drops any .Run symbol or package re-export is caught here
- [ ] __main__.run attr asserted
- [ ] Green on current tree

### NBA-0e — Pin writeSolutionsAndKernelsTCL live-path return contract and normalize __init__ facade

- **Size:** S | **Parallel group:** B | **Depends on:** NBA-0

**Summary.** VERIFIED run() calls writeSolutionsAndKernelsTCL (line 1036). Strengthen its return-shape pin (3-tuple count/uniqueKernels/results) so the later Tuning move is verifiable, and make __init__.py the explicit (still 6-name) facade for future per-module sourcing. Behavior-preserving.

**Files touched:**
- `projects/hipblaslt/tensilelite/Tensile/Tests/unit/characterization/TensileCreateLibraryRun/test_r7_createlib_deep_char.py`
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/__init__.py`

**Steps:**
1. Read writeSolutionsAndKernelsTCL (Run.py 558-665) and the existing direct calls at test_r7 lines 338/376/402/434 to find covered branches.
2. Add/strengthen a case asserting the 3-tuple return shape and that helper + CO build steps fire, using M.writeSolutionsAndKernelsTCL (RULE C) and the file's existing toolchain mocks.
3. Rewrite __init__.py from one line into an explicit multiline import of the same 6 names from .Run (behavior-preserving; resolved symbol set unchanged, verified by NBA-0d).
4. Run import-smoke + the TCLR suite.

**Build safety.** Additive pin against the in-place function; __init__ change is a no-op on the resolved symbol set (same 6 names, same source). Live run() path stays byte-identical.

**Test strategy.** Extend test_r7_createlib_deep_char.py (only file calling writeSolutionsAndKernelsTCL directly); NBA-0d guards the __init__ surface.

**ADR action.** new ADR 0004-pin-tcl-emitter-and-normalize-facade.md

**Acceptance criteria:**
- [ ] TCL 3-tuple shape asserted on a real call via M.writeSolutionsAndKernelsTCL
- [ ] __init__ still re-exports all 6 names (NBA-0d green)
- [ ] import-smoke OK

## Stage 1

Single serialization point: create the three empty target modules (IO.py, Logic.py, Tuning.py) as importable stubs. Everything in later stages depends on this one commit. No symbols move; no premature from .Run / from .IO edges added.

### NBA-1 — Scaffold empty IO.py, Logic.py, Tuning.py importable from the package

- **Size:** XS | **Parallel group:** C | **Depends on:** NBA-0

**Summary.** Add three new modules under TensileCreateLibrary/ with only a docstring each, and confirm the package imports them cleanly. No symbols move yet; this is the ONE serialization point before the parallel leaf extractions.

**Files touched:**
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/IO.py`
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Logic.py`
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Tuning.py`

**Steps:**
1. Create IO.py, Logic.py, Tuning.py each with a module docstring stating its nba-final responsibility and nothing else (no from .Run / from .IO yet, to avoid premature cycles).
2. Confirm python -c 'import Tensile.TensileCreateLibrary.IO; ...Logic; ...Tuning' succeeds.
3. Confirm Run.py and __init__.py are byte-unchanged in this commit.

**Build safety.** New empty modules are imported by nothing; Run.py and __init__ untouched, so all existing import paths and tests are byte-for-byte unaffected.

**Test strategy.** Extended import-smoke: python -c 'import Tensile.TensileCreateLibrary.{IO,Logic,Tuning}'. Run collection on TensileCreateLibraryRun/ to confirm no new collection error.

**ADR action.** update ADR 0001 (History: scaffold modules created)

**Acceptance criteria:**
- [ ] IO.py, Logic.py, Tuning.py import without error
- [ ] TensileCreateLibraryRun char tests still collect and pass
- [ ] Run.py and __init__.py unchanged in this commit

## Stage 2

Maximum-parallelism leaf extractions. Each issue moves a disjoint cohesive group from Run.py into its target module and adds exactly one back-import line into Run.py. NBA-2/4/5/6 are fully independent (disjoint Run line ranges, disjoint test files). NBA-3 (stinky) carries a same-commit monkeypatch RETARGET to IO because its functions read monkeypatched globals. NBA-7 depends on NBA-4 (needs memDecompress in IO) and uses the define-before-backimport ordering for KernelMinResult. The shared NamedTuple/processKernelSource contract stays in Run.

### NBA-2 — Extract path helpers (libraryRoot, libraryDir, _baseArchs, tensileLibraryFile) to IO.py with Run re-export

- **Size:** S | **Parallel group:** D | **Depends on:** NBA-0d, NBA-1

**Summary.** Move the four per-base-arch path helpers (Run.py 87-129) into IO.py and re-import into Run. libraryRoot/libraryDir/tensileLibraryFile are package re-exports consumed from the root by ClientWriter/BenchmarkProblems/GenerateSummations; _baseArchs is test-bound. Preserve libraryDir colon-strip semantics exactly (matched at runtime by tensile_host.cpp).

**Files touched:**
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/IO.py`
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Run.py`

**Steps:**
1. Cut libraryRoot, libraryDir, _baseArchs, tensileLibraryFile (Run.py 87-129) verbatim into IO.py with pathlib.Path / typing imports; preserve signatures and the colon-strip logic.
2. Append `from .IO import libraryRoot, libraryDir, _baseArchs, tensileLibraryFile` to the single EOF back-import block (RULE I), one name per line.
3. Verify __init__.py still re-exports libraryDir/libraryRoot/tensileLibraryFile from .Run (chain: __init__ -> .Run -> .IO).
4. Run import-smoke + test_library_paths.py + the NBA-0d surface pin.

**Build safety.** RULE A: symbols re-imported into Run same-commit, so M.libraryDir and from .Run import resolve. RULE B: __init__ chain intact; external consumers import from the package root (unchanged). No monkeypatched global moves, so RULE D unaffected.

**Test strategy.** Tensile/Tests/unit/test_library_paths.py (covers _baseArchs, libraryDir, libraryRoot, tensileLibraryFile) + NBA-0d surface pin + GenerateSummations/test_generate_summations_char.py (tensileLibraryFile via facade).

**ADR action.** new ADR 0005-extract-path-helpers-to-io.md

**Acceptance criteria:**
- [ ] IO.py defines the four helpers; Run.py re-imports them
- [ ] test_library_paths.py + NBA-0d pin green
- [ ] from Tensile.TensileCreateLibrary import libraryDir,libraryRoot,tensileLibraryFile still works
- [ ] colon-strip semantics unchanged (diff shows no logic change)

### NBA-3 — Extract stinky-asm ELF verification helpers to IO.py with Run re-export AND same-commit monkeypatch retarget

- **Size:** S | **Parallel group:** D | **Depends on:** NBA-1, NBA-2

**Summary.** Move _stinky_asm_verify_wanted, _stinky_out, _verify_stinky_asm_comment_vs_elf_text (Run.py 151-207) and the verify_stinky_paths import to IO.py. CRITICAL (resolves the proposal-1/3 contradiction): these read verify_stinky_paths/_stinky_out/globalParameters/isaToGfx as module globals, and the orchestration test patches them on M (Run). Once moved, those lookups resolve in IO, so the SAME commit must retarget the monkeypatches to the IO module (RULE C permits same-commit test edit for a move).

**Files touched:**
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/IO.py`
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Run.py`
- `projects/hipblaslt/tensilelite/Tensile/Tests/unit/characterization/TensileCreateLibraryRun/test_run_orchestration_char.py`

**Steps:**
1. Cut the three functions (Run.py 151-207) into IO.py with deps: os, pathlib.Path, globalParameters, isaToGfx, printExit, and move `from Tensile.verify_stinky_comment_vs_elf_text import verify_stinky_paths` into IO.py.
2. Add `from .IO import _stinky_asm_verify_wanted, _stinky_out, _verify_stinky_asm_comment_vs_elf_text` to Run.py so M._stinky_* resolve and the (still-in-Run) emitter closures call them via Run namespace.
3. In test_run_orchestration_char.py, add IO=import_module('...IO') and RETARGET in the SAME commit: monkeypatch.setattr(IO,'verify_stinky_paths',...), setattr(IO,'_stinky_out',...), setattr(IO,'isaToGfx',...), setitem(IO.globalParameters,...) for the cases that exercise the moved functions (lines ~96-177). This is mandatory because the function reads its own module's globals, not Run's.
4. Run import-smoke + test_run_orchestration_char.py + test_run_helpers_char.py.

**Build safety.** RULE C: underscore names re-imported into Run for tests using M.; the emitter closures (still in Run) resolve via Run namespace. RULE D resolved correctly: monkeypatches retargeted to IO in the same commit so the patched globals are the ones the moved functions actually read — this is the fix for the proposal-3 NBA-6 defect (Run keeping the import does NOT make M.verify_stinky_paths the lookup the moved function uses).

**Test strategy.** test_run_orchestration_char.py (verify_stinky_paths/_stinky_out/isaToGfx/globalParameters cases, retargeted to IO) + test_run_helpers_char.py (_stinky_out, _stinky_asm_verify_wanted via M.).

**ADR action.** new ADR 0006-extract-stinky-verify-to-io.md (documents the monkeypatch-retarget requirement)

**Acceptance criteria:**
- [ ] M._stinky_out, M._stinky_asm_verify_wanted, M._verify_stinky_asm_comment_vs_elf_text resolve
- [ ] Orchestration test monkeypatches retargeted to IO; all stinky cases pass with the patch actually effective
- [ ] test_run_helpers_char.py green
- [ ] import-smoke OK

### NBA-4 — Extract mem-compression helpers (memCompress, memDecompress) to IO.py with Run re-export

- **Size:** XS | **Parallel group:** D | **Depends on:** NBA-1

**Summary.** Move memCompress/memDecompress (Run.py 210-214) into IO.py; re-import into Run so processKernelSource (staying in Run) and tests keep resolving them. These read no monkeypatched global, so no retarget needed.

**Files touched:**
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/IO.py`
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Run.py`

**Steps:**
1. Cut memCompress and memDecompress into IO.py with pickle, zlib imports.
2. Add `from .IO import memCompress, memDecompress` to Run.py; processKernelSource (stays in Run) keeps calling memCompress via the back-import.
3. Run import-smoke + helpers + r7 char suites.

**Build safety.** RULE C: both are test-bound; re-import keeps them Run attributes. No cycle (IO defines, Run imports). No monkeypatched global moves.

**Test strategy.** test_run_helpers_char.py (memCompress, memDecompress) + test_r7_createlib_deep_char.py (memCompress).

**ADR action.** new ADR 0007-extract-mem-compression-to-io.md

**Acceptance criteria:**
- [ ] M.memCompress, M.memDecompress resolve
- [ ] test_run_helpers_char.py + test_r7 memCompress assertions green
- [ ] import-smoke OK

### NBA-5 — Extract validity-pruning + post-kernel-to-solution passes to Tuning.py with Run re-export

- **Size:** S | **Parallel group:** D | **Depends on:** NBA-1

**Summary.** Move _checkInvalidSolutionsAndKernels, _checkInvalidSolutions (dead but test-bound — DO NOT delete per scope-discipline), removeInvalidSolutionsAndKernels, passPostKernelInfoToSolution (Run.py 242-306) into Tuning.py; re-import into Run. Preserve develop's getKernelNameMin keying in passPostKernelInfoToSolution (NOT template's getKeyNoInternalArgs).

**Files touched:**
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Tuning.py`
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Run.py`

**Steps:**
1. Cut the four functions (Run.py 242-306) into Tuning.py with deps: ParallelMap2, functools.partial, printExit, tqdm, getKeyNoInternalArgs, getKernelNameMin.
2. Keep _checkInvalidSolutions verbatim even though dead (user CLAUDE.md: no deleting dead code outside the active change); flag it in the ADR for the owner.
3. Add `from .Tuning import _checkInvalidSolutionsAndKernels, _checkInvalidSolutions, removeInvalidSolutionsAndKernels, passPostKernelInfoToSolution` to Run.py.
4. Preserve develop getKernelNameMin keying.
5. Run import-smoke + helpers char suite.

**Build safety.** RULE C: all four (incl. the dead test-bound one) re-imported so M.* resolve. ParallelMap2 worker _checkInvalidSolutionsAndKernels stays top-level-importable in Tuning (picklable). No monkeypatched global moves.

**Test strategy.** test_run_helpers_char.py (passPostKernelInfoToSolution, removeInvalidSolutionsAndKernels, _checkInvalidSolutions, _checkInvalidSolutionsAndKernels).

**ADR action.** new ADR 0008-extract-validity-pruning-to-tuning.md (flag _checkInvalidSolutions dead, deferred to owner)

**Acceptance criteria:**
- [ ] M.removeInvalidSolutionsAndKernels, M.passPostKernelInfoToSolution, M._checkInvalidSolutions, M._checkInvalidSolutionsAndKernels resolve
- [ ] getKernelNameMin keying preserved (diff shows no logic change)
- [ ] test_run_helpers_char.py green
- [ ] import-smoke OK

### NBA-6 — Extract fallback-rename helpers (_renameFallbackPlaceholders, renameFallbacksPerArch) to Logic.py with Run re-export

- **Size:** S | **Parallel group:** D | **Depends on:** NBA-1

**Summary.** Move the recursive placeholder-rename and per-arch fallback deep-copy helpers (Run.py 742-794) into Logic.py; re-import into Run. They read no monkeypatched global; generateLogicDataAndSolutions (extracted later) calls renameFallbacksPerArch.

**Files touched:**
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Logic.py`
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Run.py`

**Steps:**
1. Cut _renameFallbackPlaceholders and renameFallbacksPerArch into Logic.py with deps copy.deepcopy, PlaceholderLibrary.
2. Add `from .Logic import _renameFallbackPlaceholders, renameFallbacksPerArch` to Run.py; generateLogicDataAndSolutions (still in Run for now) calls renameFallbacksPerArch via the back-import; ordering unchanged.
3. Run import-smoke + test_perArchFallbackRename.py + helpers/orchestration char suites.

**Build safety.** RULE C: both test-bound (test_perArchFallbackRename imports renameFallbacksPerArch via from ...Run import; helpers/orchestration use M.). Re-import keeps them Run attributes. No monkeypatched global moves.

**Test strategy.** test_perArchFallbackRename.py (renameFallbacksPerArch); test_run_helpers_char.py + test_run_orchestration_char.py (both helpers).

**ADR action.** new ADR 0009-extract-fallback-rename-to-logic.md

**Acceptance criteria:**
- [ ] M.renameFallbacksPerArch, M._renameFallbackPlaceholders resolve
- [ ] from ...Run import renameFallbacksPerArch still works
- [ ] test_perArchFallbackRename.py + helpers/orchestration char suites green
- [ ] import-smoke OK

### NBA-7 — Extract writeAssembly + writeHelpers to IO.py with Run re-export (define-before-backimport for KernelMinResult)

- **Size:** S | **Parallel group:** D | **Depends on:** NBA-1, NBA-4

**Summary.** Move the two disk-write primitives (Run.py 375-415) into IO.py. writeAssembly constructs KernelMinResult, which STAYS in Run — IO does `from .Run import KernelMinResult`, acyclic because KernelMinResult is defined above Run's back-import line. Depends on NBA-4 (memDecompress already in IO).

**Files touched:**
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/IO.py`
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Run.py`

**Steps:**
1. Cut writeHelpers (390-415; deps os.path, CHeader, KERNEL_HELPER_FILENAME_CPP/H) and writeAssembly (375-388; deps printExit, pathlib.Path, memDecompress, KernelMinResult) into IO.py; writeAssembly calls IO-local memDecompress (from NBA-4).
2. In IO.py add `from .Run import KernelMinResult`.
3. Per RULE I the back-import block is at EOF, so KernelMinResult (def @144) is trivially above it; IO's `from .Run import KernelMinResult` resolves against the by-then fully-defined Run when the EOF block triggers IO's import. No mid-file placement needed.
4. Append `from .IO import writeHelpers, writeAssembly` to the EOF back-import block (RULE I).
5. Run import-smoke FIRST (cycle proof) then test_r7.

**Build safety.** Acyclic-by-ordering: KernelMinResult defined in Run before Run imports from IO; IO's from .Run import KernelMinResult runs during Run's import after that binding exists. RULE C: writeAssembly/writeHelpers re-imported so M.* resolve. No monkeypatched global moves.

**Test strategy.** test_r7_createlib_deep_char.py (writeAssembly, writeHelpers, KernelMinResult). Import-smoke FIRST to catch any circular-import abort.

**ADR action.** new ADR 0010-extract-write-primitives-to-io.md (documents the define-before-backimport cycle-break)

**Acceptance criteria:**
- [ ] import Tensile.TensileCreateLibrary.Run succeeds (no circular ImportError)
- [ ] M.writeAssembly, M.writeHelpers resolve; M.KernelMinResult still resolves
- [ ] test_r7_createlib_deep_char.py green
- [ ] import-smoke OK

### NBA-8 — Extract generateKernelObjectsFromSolutions to Logic.py with Run re-export (byte-identical signature)

- **Size:** S | **Parallel group:** D | **Depends on:** NBA-1

**Summary.** Move the unique-kernel dedup derivation (Run.py 686-696, @timing) into Logic.py; re-import into Run. VERIFIED chokepoint: _codegen/codegen_harness.py + config_harness.py do `from Tensile.TensileCreateLibrary.Run import generateKernelObjectsFromSolutions` — the back-import keeps that path working; signature MUST stay byte-identical (RULE G).

**Files touched:**
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Logic.py`
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Run.py`

**Steps:**
1. Cut generateKernelObjectsFromSolutions into Logic.py with deps timing decorator, getKeyNoInternalArgs; keep the exact (solutions) -> list signature.
2. Add `from .Logic import generateKernelObjectsFromSolutions` to Run.py so both M. and the harness `from ...Run import` resolve.
3. Do NOT move generateKernelHelperObjects here (placement decided in NBA-14).
4. Run import-smoke + _codegen harness suites + LocalRead + orchestration char suite.

**Build safety.** RULE G: signature unchanged; the harness `from ...Run import` chokepoint still resolves via the back-import. No monkeypatched global moves.

**Test strategy.** _codegen/test_r2_kwconv_char.py, test_r3_kwfeat_char.py, test_r4_localread3_char.py; LocalRead/test_r3_localread_char.py; TensileCreateLibraryRun/test_run_orchestration_char.py.

**ADR action.** new ADR 0011-extract-generatekernelobjects-to-logic.md

**Acceptance criteria:**
- [ ] M.generateKernelObjectsFromSolutions resolves; signature byte-identical
- [ ] _codegen + LocalRead suites green
- [ ] orchestration char suite green
- [ ] import-smoke OK

## Stage 3

Extract the larger Logic/IO bodies that depend on stage-2 leaves. copyStaticFiles->IO is independent (parallel to the Logic work). generateLogicDataAndSolutions->Logic carries a same-commit LibraryIO monkeypatch retarget. These serialize where they share a file but are mutually parallel across IO vs Logic.

### NBA-9 — Extract copyStaticFiles to IO.py with Run re-export

- **Size:** S | **Parallel group:** E | **Depends on:** NBA-2

**Summary.** Move the static-file copier (Run.py 669-682, @timing, public package re-export) to IO.py; re-import into Run. Preserve develop's static-file list exactly (differs from template — no tensile_float8_bfloat8_bc.h).

**Files touched:**
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/IO.py`
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Run.py`

**Steps:**
1. Cut copyStaticFiles into IO.py including @timing, with shutil.copy, os.path.join, SOURCE_PATH.
2. Preserve develop's static-file list verbatim.
3. Add `from .IO import copyStaticFiles` to Run.py so both M.copyStaticFiles and the __init__ package re-export (consumed by ClientWriter/BenchmarkProblems from the root) resolve.
4. Run import-smoke + r7 char suite + NBA-0d surface pin.

**Build safety.** RULE B: re-import keeps .Run.copyStaticFiles valid, so the __init__ re-export stays valid for the two external root-consumers. @timing + develop static list preserved.

**Test strategy.** test_r7_createlib_deep_char.py (copyStaticFiles) + NBA-0d surface pin.

**ADR action.** new ADR 0012-extract-copystaticfiles-to-io.md

**Acceptance criteria:**
- [ ] copyStaticFiles in IO.py with @timing and develop static-file list intact
- [ ] Package re-export still resolves (NBA-0d green)
- [ ] test_r7 green
- [ ] import-smoke OK

### NBA-10 — Extract passPostKernelInfoToLibrary to Tuning.py with Run re-export

- **Size:** M | **Parallel group:** E | **Depends on:** NBA-0c, NBA-5

**Summary.** Move the 66-LOC develop-only library write-back (Run.py 308-373) into Tuning.py; re-import into Run. GATED on NBA-0c so the move is behaviorally verified (it was previously only patch.object'd no-op). Preserve every develop-only sizeMapping field and the KeyError diagnostic verbatim.

**Files touched:**
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Tuning.py`
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Run.py`

**Steps:**
1. Cut passPostKernelInfoToLibrary into Tuning.py with dep getKernelFileBase.
2. Add `from .Tuning import passPostKernelInfoToLibrary` to Run.py so M.passPostKernelInfoToLibrary resolves (test_r7 patch.object(M,...) target still resolves) and run()'s call site (line 1065) resolves.
3. Diff to confirm every develop-only sizeMapping field (PrefetchGlobalRead, NonTemporalA/B/D, adaptiveGemmNTAB, WaveSeparateGlobalReadA/B, UnrollLoopSwapGlobalReadOrder, DirectToVgprA/B, cuoccupancy, mathclk) copied unchanged.
4. Run import-smoke + the NBA-0c behavioral pin + r7 deep char suite.
5. Depends on NBA-5 only to order Tuning.py edits (avoid conflict), not for logic.

**Build safety.** Re-import keeps M.passPostKernelInfoToLibrary bindable (patch.object in test_r7 still works) AND the NBA-0c direct pin verifies real behavior is unchanged — the move is now behaviorally guarded, which is exactly why NBA-0c is a hard dependency. No monkeypatched global moves.

**Test strategy.** test_run_helpers_char.py (NBA-0c direct pin) + test_r7_createlib_deep_char.py (patch-target resolution + run() path).

**ADR action.** new ADR 0013-extract-passpostkernelinfotolibrary-to-tuning.md

**Acceptance criteria:**
- [ ] M.passPostKernelInfoToLibrary resolves; run() still calls it
- [ ] NBA-0c behavioral pin green after move
- [ ] sizeMapping field set preserved (verified by diff)
- [ ] test_r7 patch.object(M,...) still resolves
- [ ] import-smoke OK

### NBA-11 — Extract generateLogicDataAndSolutions to Logic.py with Run re-export AND same-commit LibraryIO monkeypatch retarget

- **Size:** M | **Parallel group:** E | **Depends on:** NBA-6

**Summary.** Move the ~120-LOC logic-load + per-arch merge + reindex + fallback + codeObjectFilesIndex core (Run.py 798-919) into Logic.py; re-import into Run. It calls renameFallbacksPerArch (now in Logic from NBA-6, intra-module) and reads LibraryIO. VERIFIED the orchestration test does monkeypatch.setattr(M.LibraryIO,'write',...) — once the function lives in Logic it reads Logic.LibraryIO, so the SAME commit must retarget that monkeypatch (RULE C/D).

**Files touched:**
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Logic.py`
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Run.py`
- `projects/hipblaslt/tensilelite/Tensile/Tests/unit/characterization/TensileCreateLibraryRun/test_run_orchestration_char.py`

**Steps:**
1. Cut generateLogicDataAndSolutions into Logic.py with deps: timing, itertools.repeat, ParallelMap2, LibraryIO (import the module), mergeTypeMismatchCollector, printTypeMismatchSummary, MasterSolutionLibrary, Assembler, print1; renameFallbacksPerArch is already Logic-local (NBA-6).
2. Add `from .Logic import generateLogicDataAndSolutions` to Run.py so M.generateLogicDataAndSolutions resolves and run() calls it via the back-import.
3. CRITICAL RULE D: in test_run_orchestration_char.py add Logic=import_module('...Logic') and retarget setattr(M.LibraryIO,'write',fake_write) -> setattr(Logic.LibraryIO,'write',fake_write) in the SAME commit (line ~315). Keep `from Tensile import LibraryIO` in Run.py because run() itself still calls LibraryIO.write, so M.LibraryIO stays a Run attribute for any other use.
4. Run import-smoke + orchestration char suite + r7 deep char suite.

**Build safety.** RULE A: re-imported into Run (M. resolves). RULE D resolved correctly: the LibraryIO.write monkeypatch is retargeted to Logic in the same commit so it patches the module the moved function actually reads; Run keeps its own LibraryIO import for run()'s direct use. Depends on NBA-6 so the intra-Logic renameFallbacksPerArch call resolves without a Run round-trip.

**Test strategy.** test_run_orchestration_char.py (generateLogicDataAndSolutions + LibraryIO.write retargeted to Logic) + test_r7_createlib_deep_char.py (run() path).

**ADR action.** new ADR 0014-extract-generatelogicdataandsolutions-to-logic.md (documents the LibraryIO monkeypatch-target migration)

**Acceptance criteria:**
- [ ] M.generateLogicDataAndSolutions resolves
- [ ] orchestration test passes with LibraryIO.write monkeypatch retargeted to Logic and effective
- [ ] M.LibraryIO still a Run attribute (run() use unaffected)
- [ ] test_r7 green
- [ ] import-smoke OK

## Stage 4

Extract the two large emitters into Tuning.py — the highest-fanout, highest-risk functions. They depend on IO helpers (writeAssembly/writeHelpers/_stinky_*/libraryDir) and on Run-resident processKernelSource/KernelCodeGenResult. They serialize after all leaf extractions and use the same define-before-backimport cycle break. writeSolutionsAndKernelsTCL is the LIVE path run() calls; writeSolutionsAndKernels is the public BenchmarkProblems-only emitter.

### NBA-12 — Extract writeSolutionsAndKernelsTCL (live build emitter) to Tuning.py with Run re-export

- **Size:** M | **Parallel group:** F | **Depends on:** NBA-2, NBA-3, NBA-5, NBA-7, NBA-0e

**Summary.** Move the process-parallel TCL emitter run() actually calls at line 1036 (Run.py 558-665) into Tuning.py; re-import into Run. It imports IO helpers and Run-resident processKernelSource/KernelCodeGenResult cross-module; acyclic by define-before-backimport ordering.

**Files touched:**
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Tuning.py`
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Run.py`

**Steps:**
1. Cut writeSolutionsAndKernelsTCL into Tuning.py with its inner assemble/compose closures.
2. In Tuning.py add `from .IO import libraryRoot, libraryDir, _baseArchs, writeAssembly, writeHelpers, _stinky_asm_verify_wanted, _verify_stinky_asm_comment_vs_elf_text` and `from .Run import processKernelSource, KernelCodeGenResult`; passPostKernelInfoToSolution/removeInvalidSolutionsAndKernels are already Tuning-local (NBA-5).
3. Per RULE I the back-import block is at EOF, so processKernelSource (@216) + KernelCodeGenResult (@132) are defined above it; Tuning's `from .Run import processKernelSource, KernelCodeGenResult` resolves when the EOF block triggers Tuning's import.
4. Append `from .Tuning import writeSolutionsAndKernelsTCL` to the EOF back-import block (RULE I) so run()'s line-1036 call resolves at runtime.
5. Run import-smoke FIRST (cycle proof) + the NBA-0e TCL pin + r7 deep char suite.

**Build safety.** RULE A + RULE C: test_r7 imports it via M.; re-import keeps it a Run attribute and run()'s in-module call resolves via the back-imported global. Acyclic-by-ordering (processKernelSource/KernelCodeGenResult defined before the back-import). NBA-0e pin verifies the live 3-tuple contract is unchanged. Note: writeSolutionsAndKernelsTCL does NOT itself read a monkeypatched Run global — the stinky helpers it calls are IO-resident and already retargeted in NBA-3.

**Test strategy.** Import-smoke FIRST (cycle proof). test_r7_createlib_deep_char.py (direct calls at 338/376/402/434 + run() path) + NBA-0e return-shape pin.

**ADR action.** new ADR 0015-extract-tcl-emitter-to-tuning.md

**Acceptance criteria:**
- [ ] import Tensile.TensileCreateLibrary.Run succeeds (no cycle)
- [ ] M.writeSolutionsAndKernelsTCL resolves; run() still invokes it; NBA-0e pin green
- [ ] test_r7 direct calls green
- [ ] import-smoke OK

### NBA-13 — Extract writeSolutionsAndKernels (non-TCL public emitter) to Tuning.py with Run re-export

- **Size:** M | **Parallel group:** F | **Depends on:** NBA-12

**Summary.** Move the public writeSolutionsAndKernels (Run.py 418-555) — the package re-export consumed by BenchmarkProblems.py from the root, NOT in run()'s path — into Tuning.py; re-import into Run and confirm the __init__ facade still exposes it. Serialize after NBA-12 (shared Tuning.py + Run import-block + same cycle technique).

**Files touched:**
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Tuning.py`
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Run.py`

**Steps:**
1. Cut writeSolutionsAndKernels into Tuning.py with its two inner closures and the removeInvalidSolutionsAndKernels/passPostKernelInfoToSolution calls (now Tuning-local).
2. Reuse the sibling imports added in NBA-12 (IO helpers + from .Run import processKernelSource, KernelCodeGenResult).
3. Add `from .Tuning import writeSolutionsAndKernels` to Run.py so .Run.writeSolutionsAndKernels and thus the __init__ package re-export stay valid for BenchmarkProblems.py.
4. Run import-smoke + a BenchmarkProblems import check + TensileCreateLibraryRun suite + NBA-0d surface pin.

**Build safety.** RULE B: __init__ chain intact (BenchmarkProblems imports from the root, which is preserved). Acyclic-by-ordering identical to NBA-12. Not in run()'s path, so the NBA-0d package-re-export pin + the BenchmarkProblems import smoke are the catch (the run() suite won't exercise it).

**Test strategy.** Import-smoke (cycle proof) + BenchmarkProblems import smoke + NBA-0d surface pin + TensileCreateLibraryRun/test_run_helpers_char.py.

**ADR action.** new ADR 0016-extract-legacy-emitter-to-tuning.md

**Acceptance criteria:**
- [ ] import Tensile.TensileCreateLibrary.Run succeeds (no cycle)
- [ ] M.writeSolutionsAndKernels resolves; from Tensile.TensileCreateLibrary import writeSolutionsAndKernels works (NBA-0d green)
- [ ] BenchmarkProblems import smoke green
- [ ] import-smoke OK

## Stage 5

Finalize: record the one ambiguous placement decision (generateKernelHelperObjects), slim Run.py to the orchestration core + shared contract + back-import block, remove now-dead imports, and run the single full-batch no-regression gate against the confirmed baseline. Then OPTIONAL consumer-import cleanup (default-skippable; shims work indefinitely).

### NBA-14 — Record placement decision for generateKernelHelperObjects (keep in Run, ADR only)

- **Size:** XS | **Parallel group:** G | **Depends on:** NBA-8

**Summary.** Resolve the one ambiguous placement: keep generateKernelHelperObjects in Run.py (template fidelity; VERIFIED the _codegen harness imports it via from ...Run import). Documentation-only, no code move, so the chokepoint import is undisturbed.

**Files touched:**
- `projects/hipblaslt/tensilelite/docs/decisions/0017-helper-objects-placement.md`

**Steps:**
1. Confirm _codegen/codegen_harness.py imports generateKernelHelperObjects from Tensile.TensileCreateLibrary.Run (RULE G).
2. Decide keep-in-Run; write ADR 0017 recording the decision, the named alternative (Logic by listing-rule + re-export), and the condition under which Logic would win (if Run.py is later converted to a package).
3. Run the _codegen suites to confirm nothing changed.

**Build safety.** Documentation-only (no code move); the chokepoint import is untouched, so the build is trivially green.

**Test strategy.** _codegen/test_r2_kwconv_char.py, test_r3_kwfeat_char.py (cover generateKernelHelperObjects) confirm no regression.

**ADR action.** new ADR 0017-helper-objects-placement.md

**Acceptance criteria:**
- [ ] ADR 0017 records the keep-in-Run decision with the named alternative
- [ ] _codegen suites green

### NBA-15 — Slim Run.py to orchestration core + back-imports; full no-regression gate

- **Size:** S | **Parallel group:** G | **Depends on:** NBA-7, NBA-9, NBA-10, NBA-11, NBA-13, NBA-14

**Summary.** Verify Run.py now defines only run(), processKernelSource, KernelCodeGenResult, KernelMinResult, generateKernelHelperObjects (per NBA-14), and the consolidated back-import block; remove now-dead top-level imports (grep-verified), keep RULE D imports (os/shutil/LibraryIO/globalParameters). Run the full -m unit gate vs the NBA-0b baseline.

**Files touched:**
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Run.py`

**Steps:**
1. grep each candidate dead import in Run.py before removing (e.g. zlib/pickle if processKernelSource no longer references them; copy if generateLogicDataAndSolutions moved); remove only those with zero remaining references.
2. Explicitly RETAIN os, shutil, `from Tensile import LibraryIO`, globalParameters at module level (run() still uses LibraryIO.write; tests still read M.os/M.shutil/M.globalParameters — RULE D).
3. Confirm the back-import block sits at the bottom (below KernelMinResult/KernelCodeGenResult/processKernelSource defs).
4. Run the full gate tox -e unit -- -m unit Tensile/Tests/unit and compare to NBA-0b baseline; run TensileCreateLibraryRun + _codegen + LocalRead + ParseArguments char suites + test_library_paths.py + test_perArchFallbackRename.py.

**Build safety.** Only grep-verified dead-import removal; RULE D imports explicitly retained. The full gate matching the NBA-0b baseline is the proof.

**Test strategy.** Full gate: tox -e unit -- -m unit Tensile/Tests/unit (compare to NBA-0b). Plus all named char suites + test_library_paths.py + test_perArchFallbackRename.py.

**ADR action.** new ADR 0018-slim-run-orchestration-core.md (records final Run.py LOC + full-gate count vs NBA-0b baseline + module map)

**Acceptance criteria:**
- [ ] Full -m unit count equals the NBA-0b confirmed baseline (no regression)
- [ ] M.os, M.shutil, M.LibraryIO, M.globalParameters still resolve (RULE D)
- [ ] Run.py defines only run, processKernelSource, the two NamedTuples, generateKernelHelperObjects + back-imports
- [ ] import-smoke OK

### NBA-16 — OPTIONAL: repoint __init__ facade to true source modules and migrate consumer imports

- **Size:** S | **Parallel group:** H | **Depends on:** NBA-15

**Summary.** Default-skippable cleanup: point __init__.py at real homes (libraryDir/libraryRoot/tensileLibraryFile from .IO; copyStaticFiles from .IO; writeSolutionsAndKernels from .Tuning; run from .Run) and optionally migrate char tests off the Run back-import shims per symbol group, removing each group's shim in the same commit. Recommended default: keep the shims and record that decision.

**Files touched:**
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/__init__.py`
- `projects/hipblaslt/tensilelite/Tensile/TensileCreateLibrary/Run.py`
- `projects/hipblaslt/tensilelite/Tensile/Tests/unit/characterization/TensileCreateLibraryRun/test_run_helpers_char.py`
- `projects/hipblaslt/tensilelite/Tensile/Tests/unit/test_library_paths.py`

**Steps:**
1. Rewrite __init__ imports to source the 6 names from their real homes (run from .Run; copyStaticFiles/libraryDir/libraryRoot/tensileLibraryFile from .IO; writeSolutionsAndKernels from .Tuning), keeping the Run back-imports so M.-based tests stay green; verify NBA-0d + the 3 external root-consumers still resolve.
2. OPTIONAL per-group: migrate ONE symbol group's tests to import_module the real module AND remove only that group's Run back-import in the SAME commit; re-run that group's char tests; repeat. RULE C: test edit + shim removal land together.
3. Recommended default: STOP after the facade repoint, keep all Run shims (zero-risk), and record that decision in an ADR rather than churning tests.
4. Full gate once at the end of the stage.

**Build safety.** Facade repoint keeps the same 6 names (only source path changes); Run back-imports stay so M.-based tests are untouched. Any optional per-group migration removes a shim only in the same commit as its test migration, so no consumer ever points at a removed attribute.

**Test strategy.** NBA-0d surface pin + GenerateSummations/test_generate_summations_char.py + ClientPath/test_clientwriter_path_char.py + per-group char tests for any migrated group; full -m unit gate at stage end.

**ADR action.** new ADR 0019-consumer-import-cleanup.md (records facade repoint + the keep-shims-vs-migrate decision)

**Acceptance criteria:**
- [ ] from Tensile.TensileCreateLibrary import all 6 names works (NBA-0d green)
- [ ] GenerateSummations + ClientPath char tests green
- [ ] No consumer references a removed Run attribute
- [ ] Full -m unit gate matches the NBA-0b baseline

